{
  "name": "byjtt-design",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {"node": ">=22"},
  "scripts": {
    "test": "npm run shell:contract && npm run foundation:validate && npm run benchmark:validate && npm run shell:idempotent && node --test app/model.test.mjs app/studio.test.mjs app/library.test.mjs app/specimen.test.mjs app/design-contract.test.mjs app/decision-trace.test.mjs app/context-package.test.mjs app/evidence.test.mjs",
    "test:browser": "npm run apply:shell && playwright test public-shell.spec.mjs --config=playwright.public.config.mjs",
    "copy:lint": "node harness/copy-lint.mjs",
    "build": "npm run shell:contract && npm run foundation:validate && npm run shell:idempotent && npm run copy:lint",
    "shell:contract": "node harness/validate-shell.mjs --contract-only",
    "shell:idempotent": "node -e \"import('./harness/apply-public-shell.mjs').then(({checkShellIdempotence})=>checkShellIdempotence())\"",
    "apply:shell": "node harness/apply-public-shell.mjs",
    "foundation:validate": "node harness/validate-foundation.mjs",
    "shell:validate": "node harness/validate-shell-output.mjs",
    "benchmark:validate": "node harness/validate-benchmark.mjs",
    "benchmark:export-contexts": "node harness/export-benchmark-contexts.mjs",
    "benchmark:run:init": "node harness/init-benchmark-run.mjs",
    "benchmark:serve": "node harness/server.mjs",
    "benchmark:test": "playwright test",
    "benchmark:candidate:smoke": "node harness/run-candidate-evaluator.mjs",
    "benchmark:candidate:evaluate": "node harness/run-candidate-evaluator.mjs",
    "benchmark:check": "npm run benchmark:validate && npm run benchmark:export-contexts && npm run benchmark:test && npm run benchmark:candidate:smoke"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.62.1",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1"
  }
}
