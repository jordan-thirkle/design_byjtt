# ByJTT-guided v0 prompt

Read `SHARED-CONTROLS.md`, `brief.json`, `fixtures/analytics.json` and every additional design/product source supplied in this bundle.

Build the requested operational analytics workspace.

Apply the ByJTT treatment in this order before and during implementation:

1. **Requirements discipline** — translate the brief into explicit user jobs, information hierarchy, states, constraints and acceptance criteria before visual implementation.
2. **Solved-system gate** — prefer mature accessible primitives or capabilities already available in the execution environment when they materially improve correctness, accessibility or maintainability; do not provision external paid services.
3. **Design Contract** — apply the supplied `DESIGN.md` principles and rationale. Significant visual devices should have a user, product, accessibility, platform or brand rationale rather than existing because they are common generated-interface patterns.
4. **Implementation continuity** — preserve design intent through the actual runnable implementation; the rendered product, not a screenshot, is the deliverable.
5. **Pre-submission Gauntlet** — use the supplied `gauntlet.md` within the remaining attempt budget and make only evidence-backed corrections.

Do not add treatment rules that are not contained in the supplied bundle. If any supplied ByJTT guidance conflicts with the canonical brief or shared controls, the brief/shared controls win and the conflict must be recorded.
