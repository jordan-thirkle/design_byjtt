# Design Studio application

The `app/` directory contains the first browser-native vertical slice for `design.byjtt.com`.

It is intentionally dependency-light. The first slice proves the product loop with deterministic local-service specimen generation, conversational iteration, evidence, and opt-in publication. A production model/API adapter can replace `model.js` transformations without changing the product contracts.